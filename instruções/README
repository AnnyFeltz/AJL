hello, deixei comentarios pra Julia nos index e login, só são 4 arquivos então fiz 2 q são os de adicionar item e registrar usuario

e Lu, se puder fazer um cabeçalho e uns css bonitinho agradeço, ja q vc queria isso, é, provavelmente vc vai ter q esperar a julia fazer o back antes, ai vc vai poder decorar
no cabeçalho minha unica requisição é ter o botão sair nele blz?

aqui as rotas pras paginas e o logout q tem

não pus nome pq são autoexplicativos

href="{{ url_for('index') }}"
href="{{ url_for('login') }}"
href="{{ url_for('logout') }}"
href="{{ url_for('register') }}"
href="{{ url_for('add_item') }}"

ss vcs vão precisar do docker, assim q intalarem pra rodar o projeto é assim

docker-compose up -d --build

ai vocês podem acessar o site em http://localhost:8080
e o banco de dados vai estar emhttp://localhost:8081

vou deixar umas fotos aqui pra verem como é

ah é se quiserem remover tudo do docker da um 

docker-compose down

---
o docker nao rodou de jeito nenhum, entao provavelmente o problema e o9 noote msm
entao eu so mexi no codigo, mas nao sei se ta funcionando (é pra ta pq ébem simples)
se precisar arrumar eu arrumo (so depois de umas 15hrs pq to terminado de tentar fazer essa merda funcionaar faz mt tempo  E É TIPO 05:18 ksksksksksksksksksksksks)
---

Codigos pro mysql

docker exec -it mysql_db mysql -u root -p
senha: root

USE simples_api;

CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100),
    email VARCHAR(100) UNIQUE,
    password VARCHAR(255)
);

CREATE TABLE items (
  id INT AUTO_INCREMENT PRIMARY KEY,
  nome VARCHAR(255) NOT NULL,
  quantidade VARCHAR(50),
  comprado BOOLEAN DEFAULT FALSE,
  user_id INT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id)
);

USE simples_api;

INSERT INTO users (name, email, password) VALUES 
('Usuário 1', 'usuario1@gmail.com', 'senha123'),
('Usuário 2', 'usuario2@gmail.com', '123senha');

INSERT INTO items (nome, quantidade, comprado, user_id) VALUES
('Arroz', '2 kg', false, 1),
('Feijão preto', '1 kg', false, 1),
('Macarrão espaguete', '3 unidades', false, 1),
('Molho de tomate', '2 sachês', true, 1),
('Detergente', '5 frascos', false, 1),
('Papel higiênico', '12 rolos', true, 2),
('Shampoo', '1 frasco', false, 2),
('Escova de dentes', '2 unidades', true, 2),
('Sabonete', '6 unidades', false, 2),
('Desodorante', '2 frascos', true, 2);
